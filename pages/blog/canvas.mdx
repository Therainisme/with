import {BlogLayout, withGetStaticProps} from "@/components/BlogLayout";

export const title = "让我来试试 canvas";
export const date = "2022-06-07";
export const getStaticProps = withGetStaticProps(title, date);
export default (props) => {
  return BlogLayout(props);
};

import TwoIntersectingRectangles from "@/components/canvas/TwoIntersectingRectangles";
import Drawing from "@/components/canvas/Drawing";


:::note
# 写在前面
大部分是这里的内容 [Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API/Tutorial)
:::

## Basic Usage

![canvas gird](https://user-images.githubusercontent.com/41776735/172348413-650729fa-5f44-4270-a504-ae50f3f2cf88.png)

### 两个相交的矩形

```ts
const canvas = canvasRef.current;
if (canvas?.getContext) {
	const ctx = canvas.getContext('2d')!;

	ctx.fillStyle = 'rgb(200, 0, 0)';
	ctx.fillRect(10, 10, 50, 50);

	ctx.fillStyle = 'rgba(0, 0, 200, 0.5)';
	ctx.fillRect(30, 30, 50, 50);
}
```

<TwoIntersectingRectangles/>

### 试试就~~逝世~~

<Drawing/>